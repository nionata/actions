const chai = require('chai')
const expect = chai.expect
// const { createApp, deleteApp } = require('./review/main')

describe('auto', function() {
    describe('review', function() {
        it('should create an app on an open pr', async function() {

            // let response 
            // try {
            //     response = (await createApp()).data
            // } catch (error) {
            //     throw error
            // }

            // expect(response).deep.equals(topics)
        })

        it('should delete an app on a closed pr', async function() {

            // let response 
            // try {
            //     response = (await axios.get('http://localhost:1337/topics')).data
            // } catch (error) {
            //     throw error
            // }

            // expect(response).deep.equals(topics)
        })
    })
})